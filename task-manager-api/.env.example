# Copy to .env and fill in. Never commit .env.
SECRET_KEY=change-me-before-deploying
DEBUG=false
HOST=0.0.0.0
PORT=5000
DATABASE_URI=sqlite:///tasks.db
# SMTP — leave blank to disable real email sending in dev.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
