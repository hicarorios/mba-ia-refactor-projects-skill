# Copy to .env and fill in. Never commit .env.
SECRET_KEY=change-me-before-deploying
DEBUG=false
HOST=0.0.0.0
PORT=5000
DB_PATH=loja.db
APP_ENV=development
# Optional: enables the gated /admin/reset-db endpoint when set.
ADMIN_TOKEN=
